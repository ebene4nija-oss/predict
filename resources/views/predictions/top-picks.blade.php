@extends('layouts.app')

@section('title', $seoTitle ?? 'Top Correct Score & Football Predictions Today — Guaranteed Correct Score Tips')
@section('meta_description', $seoDescription ?? 'Discover daily high-confidence correct score predictions, match winner tips, and Poisson xG analytics at GuaranteedCorrectScoreTips.com.')
@section('meta_keywords', strtolower(($definition?->label ?? 'guaranteed correct score') . ' predictions, ' . ($definition?->short ?? 'correct score') . ' tips today, guaranteed correct score tips, AI betting predictions, Poisson xG betting tips, high conviction soccer picks'))
@section('canonical', $canonicalUrl ?? route('top.picks', ['market' => $market]))

@section('schema_json_ld')
@php
    $items = [];
    $visiblePicks = $isSubscriber ? $top10Picks->take(10) : $top10Picks->take($freePicks);
    foreach ($visiblePicks as $idx => $p) {
        if ($p->match) {
            $items[] = [
                '@type' => 'ListItem',
                'position' => $idx + 1,
                'name' => "{$p->match->home_team} vs {$p->match->away_team} - {$p->pick} ({$p->market})",
                'url' => $p->match->canonicalUrl(),
            ];
        }
    }

    $topPicksSchema = [
        '@context' => 'https://schema.org',
        '@type' => 'CollectionPage',
        'name' => $seoTitle ?? 'AI Top Picks',
        'description' => $seoDescription ?? 'High-confidence football predictions generated by expected-goals (xG) Poisson calculations.',
        'url' => $canonicalUrl ?? route('top.picks', ['market' => $market]),
        'mainEntity' => [
            '@type' => 'ItemList',
            'itemListElement' => $items,
        ],
    ];
@endphp
<script type="application/ld+json">
{!! json_encode($topPicksSchema, JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE) !!}
</script>
@endsection

@section('content')
    <x-ad-banner type="header" />

    <div class="space-y-8">
        <!-- Page Header -->
        <div class="flex flex-col md:flex-row items-start md:items-center justify-between gap-4">
            <div>
                <h1 class="text-3xl font-extrabold text-white">AI Ranked Top Picks</h1>
                <p class="text-xs sm:text-sm text-slate-400 mt-1">High-confidence predictions generated by expected-goals (xG) Poisson calculations.</p>
            </div>

            <!-- Segmented Control for Market Filter -->
            <div class="p-1 rounded-2xl glass-panel flex flex-wrap items-center gap-1">
                @foreach($markets as $key => $definition)
                    <a href="{{ route('top.picks', ['market' => $key]) }}"
                       class="px-3.5 py-2 rounded-xl text-xs font-bold transition-all whitespace-nowrap {{ $market === $key ? 'bg-[#38BDF8] text-slate-950 shadow-md' : ($definition->generated ? 'text-slate-400 hover:text-white' : 'text-slate-600 hover:text-slate-400') }}">
                        {{ $definition->listLabel }}
                        @unless($definition->generated)
                            <span class="ml-1 text-[9px] uppercase tracking-wide text-amber-500/80">soon</span>
                        @endunless
                    </a>
                @endforeach
            </div>
        </div>

        <!-- AI's Top 5 Elevated Hero Section -->
        @if($aiTop5->isNotEmpty())
            <div class="p-6 rounded-3xl bg-gradient-to-br from-[#151A24] via-[#121824] to-[#0A0F18] border border-sky-500/30">
                <div class="flex items-center space-x-2 text-xs font-extrabold text-[#38BDF8] mb-4">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M13 10V3L4 14h7v7l9-11h-7z"/></svg>
                    <span>AI'S TOP 5 CONVICTION PICKS (CROSS-MARKET)</span>
                </div>

                <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-5 gap-4">
                    @foreach($aiTop5 as $top)
                        @if($top->match)
                            <a href="{{ $top->match->canonicalUrl() }}" class="block p-4 rounded-2xl bg-[#0B0F17] border border-slate-800 hover:border-sky-400 transition-all group">
                                <div class="text-[10px] text-slate-400 truncate mb-1">{{ $top->match->league }}</div>
                                <div class="text-xs font-extrabold text-white group-hover:text-[#38BDF8] transition-colors truncate">
                                    {{ $top->match->home_team }} vs {{ $top->match->away_team }}
                                </div>
                                <div class="mt-3 pt-2 border-t border-slate-800 flex items-center justify-between">
                                    <span class="text-[11px] font-bold text-sky-400 truncate mr-1">{{ $top->pick }}</span>
                                    <span class="text-xs font-mono font-black text-emerald-400 shrink-0">{{ round($top->probability * 100, 1) }}%</span>
                                </div>
                            </a>
                        @endif
                    @endforeach
                </div>

                <!-- SportyBet Booking Code Integration Bar -->
                <div class="mt-4">
                    <x-sportybet-booking-code :picks="$aiTop5" variant="compact" />
                </div>
            </div>
        @endif

        <!-- Official Telegram Community Banner -->
        <x-telegram-banner />

        <!-- In-Content Ad Placement -->
        <x-ad-banner type="in-content" />

        <!-- Top 10 Ranked Picks Table / Cards -->
        <div class="space-y-4">
            <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-2">
                <h2 class="text-xl font-bold text-white flex items-center space-x-2">
                    <span>Ranked Picks for {{ get_market_label($market) }}</span>
                    <span class="text-xs font-normal text-slate-400">({{ $top10Picks->count() }} picks)</span>
                </h2>
                @if(!$isSubscriber)
                    <div class="inline-flex items-center space-x-1.5 text-xs font-medium text-amber-400 bg-amber-500/10 border border-amber-500/30 px-3 py-1 rounded-full self-start sm:self-auto">
                        <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/></svg>
                        <span>Top {{ $freePicks }} Unlocked · Picks #{{ $freePicks + 1 }}–#10 Require PRO</span>
                    </div>
                @endif
            </div>

            @unless($definition?->generated)
                <div class="p-6 rounded-2xl glass-panel border border-amber-500/20 flex items-start space-x-4">
                    <span class="p-2 rounded-xl bg-amber-500/10 text-amber-400 border border-amber-500/30 shrink-0">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                    </span>
                    <div>
                        <div class="text-sm font-bold text-white">{{ $definition?->listLabel }} is not live yet</div>
                        <p class="text-xs text-slate-400 mt-1">
                            {{ $definition?->pendingNote }}
                            We publish a market only once we can also settle it — a list we cannot grade has no track record behind it.
                        </p>
                    </div>
                </div>
            @endunless

            <div class="space-y-3">
                @foreach($top10Picks as $index => $pick)
                    @php
                        $isUnlocked = $isSubscriber || ($index < $freePicks);
                    @endphp

                    @if($isUnlocked)
                        <!-- Unlocked Pick Row -->
                        <div class="p-4 sm:p-5 rounded-2xl glass-panel flex flex-col sm:flex-row items-start sm:items-center justify-between gap-4 border border-slate-800 hover:border-slate-700 transition-all">
                            <div class="flex items-center space-x-4">
                                <div class="w-8 h-8 rounded-xl bg-slate-800 text-slate-300 flex items-center justify-center font-bold text-xs font-mono shrink-0">
                                    #{{ $index + 1 }}
                                </div>
                                <div>
                                    <div class="text-xs text-slate-400">{{ $pick->match->league }} • {{ $pick->match->kickoff_at ? $pick->match->kickoff_at->format('M d, H:i') : 'Upcoming' }}</div>
                                    <a href="{{ $pick->match->canonicalUrl() }}" class="font-extrabold text-base text-white hover:text-[#38BDF8] transition-colors">
                                        {{ $pick->match->home_team }} vs {{ $pick->match->away_team }}
                                    </a>
                                </div>
                            </div>

                            <div class="flex items-center space-x-4 w-full sm:w-auto justify-between sm:justify-end">
                                <div class="text-left sm:text-right">
                                    <div class="text-[10px] text-slate-500 uppercase font-semibold">AI Recommendation</div>
                                    <div class="text-sm font-extrabold text-[#38BDF8]">{{ $pick->pick }}</div>
                                </div>
                                <div class="text-right">
                                    <div class="text-[10px] text-slate-500 uppercase font-semibold">Confidence</div>
                                    <div class="text-base font-extrabold text-emerald-400 font-mono">{{ round($pick->probability * 100, 1) }}%</div>
                                </div>
                                <a href="{{ $pick->match->canonicalUrl() }}" class="px-3 py-1.5 rounded-lg bg-slate-900 hover:bg-slate-800 text-[#38BDF8] border border-slate-800 font-bold text-xs transition-colors whitespace-nowrap hidden sm:inline-block">
                                    Match Preview &rarr;
                                </a>
                            </div>
                        </div>
                    @else
                        <!-- Locked Pick Row (Blurred Paywall Card) -->
                        <div class="relative p-5 rounded-2xl glass-panel border border-sky-500/20 overflow-hidden group">
                            <!-- Blurred Background Content -->
                            <div class="filter blur-md select-none pointer-events-none opacity-40 flex items-center justify-between">
                                <div class="flex items-center space-x-4">
                                    <div class="w-8 h-8 rounded-xl bg-slate-800 text-slate-300 flex items-center justify-center font-bold text-xs font-mono shrink-0">
                                        #{{ $index + 1 }}
                                    </div>
                                    <div>
                                        <div class="text-xs text-slate-400">UEFA Champions League • 20:00</div>
                                        <div class="font-extrabold text-base text-white">Match {{ $index + 1 }} Locked Team A vs Team B</div>
                                    </div>
                                </div>
                                <div class="text-right">
                                    <div class="text-xs font-bold text-[#38BDF8]">Pick Locked</div>
                                    <div class="text-base font-extrabold text-emerald-400 font-mono">78.5%</div>
                                </div>
                            </div>

                            <!-- Lock Overlay & Subscribe CTA -->
                            <div class="absolute inset-0 bg-[#0B0F17]/85 backdrop-blur-sm flex flex-col sm:flex-row items-start sm:items-center justify-between gap-3 p-4 sm:px-6 sm:py-4">
                                <div class="flex items-center space-x-3">
                                    <span class="p-2 rounded-xl bg-amber-500/10 text-amber-400 border border-amber-500/30 shrink-0">
                                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/></svg>
                                    </span>
                                    <div>
                                        <div class="text-xs font-bold text-white">Pick #{{ $index + 1 }} Gated for Pro Subscribers</div>
                                        <div class="text-[11px] text-slate-400">Free members see the top {{ $freePicks }}. Upgrade to PRO to unlock all 10 ranked picks.</div>
                                    </div>
                                </div>
                                <a href="{{ route('subscription.pricing') }}" class="px-4 py-2 rounded-xl text-xs font-extrabold bg-gradient-to-r from-sky-500 to-blue-600 hover:from-sky-400 hover:to-blue-500 text-white shadow-lg shadow-sky-500/20 transition-all whitespace-nowrap self-end sm:self-auto">
                                    Unlock Now &rarr;
                                </a>
                            </div>
                        </div>
                    @endif
                @endforeach
            </div>

            @if(!$isSubscriber && $top10Picks->count() > $freePicks)
                <div class="mt-8 p-6 sm:p-8 rounded-3xl bg-gradient-to-r from-sky-950/60 via-[#151A24] to-indigo-950/60 border-2 border-sky-500/40 shadow-2xl relative overflow-hidden flex flex-col sm:flex-row items-center justify-between gap-6">
                    <div class="space-y-2 text-center sm:text-left">
                        <div class="inline-flex items-center space-x-1.5 px-3 py-0.5 rounded-full bg-sky-500/20 text-sky-300 border border-sky-500/30 text-[10px] font-extrabold uppercase tracking-wider">
                            <span>👑 VIP ACCESS</span>
                        </div>
                        <h3 class="text-lg sm:text-xl font-extrabold text-white">Unlock All 10 Ranked {{ get_market_label($market) }} Picks</h3>
                        <p class="text-xs text-slate-300 max-w-xl">
                            Upgrade to Guaranteed Correct PRO to unlock full cross-market Top 10 lists, verified human expert insights, and an ad-free experience.
                        </p>
                    </div>
                    <a href="{{ route('subscription.pricing') }}" class="px-6 py-3.5 rounded-xl bg-gradient-to-r from-sky-500 via-blue-600 to-emerald-500 hover:from-sky-400 hover:to-emerald-400 text-white font-extrabold text-xs shadow-xl shadow-sky-500/25 transition-all whitespace-nowrap">
                        Upgrade to PRO VIP &rarr;
                    </a>
                </div>
            @endif
        </div>
    </div>
@endsection
